image: gcc

build:
  stage: build

  before_script:
    - apt update && apt -y install make autoconf
  script:
    - g++ IteratorsBabcock.cpp -Wall -O2 -o Iterators
  artifacts:
    paths:
      - Iterators

